# Astartis x Codex website lab

This directory is a static Vercel-ready landing page. It deliberately has no backend connection: the interactive lab replays a clearly-labelled, deterministic simulation of the Astartis dashboard scenario.

To deploy, import the repository in Vercel and set this `site` directory as the project root. Select the **Other** framework preset and leave build/output fields empty.
